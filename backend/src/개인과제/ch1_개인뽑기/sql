drop database boardtest;
create database rand;
use rand;
drop table if exists random_choice;
create table random_choice(
	carta int primary key auto_increment,
    name_list varchar(10)
);
select * from random_choice;

insert into random_choice values(null,'김경주');